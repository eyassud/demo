---
description: Styling rules for this project — Tailwind CSS usage
paths:
  - src/**/*.svelte
---

# Styling

- Use Tailwind CSS classes over inline styles.
- Do not use CSS-in-JS libraries unless explicitly added.
- Scoped component styles go in the `<style>` block at the bottom of each `.svelte` file.
